import RoleCards from "./RoleCards/RoleCards";
import Section from "../Section";

const Role = () => (
  <Section Color="#3A7CA5" Title="Open Roles" Content={<RoleCards />} />
);

export default Role;
