import styled from "styled-components";

const StyledIdeaCards = styled.section`
  background-color: ${({ theme }) => theme.colors.NEUTRAL_2};
  padding: 58px 0 21px 0;
`

export default StyledIdeaCards;