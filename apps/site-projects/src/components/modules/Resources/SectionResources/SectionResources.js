import { SectionResourcesContainer } from "./StyledSectionResources";

function SectionResources({ children }) {
    return (
        <SectionResourcesContainer>
            {children}
        </SectionResourcesContainer>
    )
}

export default SectionResources;