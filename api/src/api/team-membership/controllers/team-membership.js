'use strict';

/**
 * team-membership controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::team-membership.team-membership');
