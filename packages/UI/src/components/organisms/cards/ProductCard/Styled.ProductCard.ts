import styled from 'styled-components';
import { radiusStyles } from '../../../atoms/Radius/StyledRadius';

export const Container = styled.div`
  width: '966px';
  border-radius: ${radiusStyles.radius300};
`;
